{% extends 'base.html.twig' %}

{% block body %}
    <h1>Medico</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medico.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ medico.nombre }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ medico.email }}</td>
            </tr>
            <tr>
                <th>Centros médicos</th>
                <td>
                    {{dump(medico)}}
                </td>
            </tr>

        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('medicos_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('medicos_edit', { 'id': medico.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
